[package]
name = "esp32-cam-streamer"
version = "0.1.0"
edition = "2021"
authors = ["Antigravity Developer"]
description = "Cross-platform ESP32-CAM MJPEG streamer with Ctrl+X image snapshot capture."

[dependencies]
eframe = "0.27.2"
egui = "0.27.2"
image = { version = "0.24", features = ["jpeg", "png"] }
chrono = "0.4"
dirs = "5.0"
